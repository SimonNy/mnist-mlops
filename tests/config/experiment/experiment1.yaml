# data and model paths
processed_dir: "data/processed"
models_dir: "models"
figures_dir: "reports/figures"

# Architecture 
input_dim: 1 
first_dim: 32 
second_dim: 64 
third_dim: 128 
output_dim: 10
dropout: 0.5

# Model training
batch_size: 32
lr: 0.03
epochs: 10
seed: 123
